// * Functional Programing
/* 
var currencyOne = 100;
var currencyTwo = 0;
var exchangeRate = 1.2;

function convertCurrency(amount, rate){
    return amount * rate;
}

currencyTwo = convertCurrency(currencyOne, exchangeRate);
console.log(currencyTwo);
*/
/*
function consoleLog(val) {
    console.log(val)
    return val
}

consoleLog("Hey there, what's shaking?")

function doubleIt(num) {
    return num * 2
}

function objectMaker(val) {
    return{
        prop: val
    }
}

objectMaker(20);

doubleIt(10).toString();

let result = objectMaker( doubleIt(100) );
console.log(result)

let counter = 3;
function example() {
    console.log(counter);
    counter = counter - 1;
    if (counter === 0) return;
    example();
}

example();

*/

